# Copy this file to terraform.tfvars and fill in your values
# DO NOT commit terraform.tfvars to git (it's in .gitignore)

aws_region  = "eu-west-1"
environment = "staging"

# Get from https://console.anthropic.com/settings/keys
claude_api_key = "sk-ant-api03-..."

# Get from https://dashboard.stripe.com/apikeys (use test keys for staging)
stripe_secret_key = "sk_test_..."

# Get from https://dashboard.stripe.com/webhooks
stripe_webhook_secret = "whsec_..."

# Update with your actual frontend URLs
cognito_callback_urls = [
  "https://staging.koumpa.com/auth/callback"
]

cognito_logout_urls = [
  "https://staging.koumpa.com"
]
